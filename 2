#include <iostream>
#include <vector>

int main(){
    int n;
    std::cin >> n ;
    char a[8][8];
    for (int i = 0; i < 8; i++) {
        for (int j = 0; j < 8; j++) {
            std::cin >> a[i][j];
        }
    } 
    for (int i = 0; i < 8; i++) {
        for (int j = 0; j < 8; j++) {
            if a[i+1][j+1] == '*' && a[i+1][j-1] == '*' &&
        
        return 0;
} 
